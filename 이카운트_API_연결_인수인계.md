# 이카운트(ECOUNT) OpenAPI 연결 — 인수인계

**프레젠스월드 온라인사업부 · 출고 작업 자동화**
**작성일:** 2026-06-16
**대상:** 이 작업을 이어받는 Claude Code (및 담당자)

> 이 문서는 **이미 완료·검증된** 이카운트 OpenAPI 연결 작업을 다른 담당자의 Claude Code가
> 그대로 이어받을 수 있도록 정리한 기술 인수인계 자료입니다. 같은 폴더의 `ecount_*.py` 가
> 실제 동작하는 코드이며, 본사 환경에서 **실서버 재고 2,038건 조회까지 검증**되었습니다.

---

## 0. 큰 그림 (왜 이걸 만드나)

- 회사는 3PL 파트너 **디아이(DI)** 로 출고를 넣는 구조이고, 출고 작업을 자동화하는 중이다.
- 출고 자동화는 여러 소스(네이버 주문 · 디아이 재고 · **이카운트 재고** · 발매일/입금요청서)를 모아
  디아이 업로드 양식을 만들고, 송장을 네이버에 역등록하는 흐름이다.
- **API 연결 경계 (중요):**
  - ✅ 네이버 스마트스토어 — 연결됨 (별도 프로젝트 `smartstore_automatic`)
  - ✅ **이카운트(ECOUNT)** — **이번에 연결 완료 (이 문서)**
  - ❌ 크림(KREAM) · 디아이 전산 — 아직 미연결 (향후 과제)
- 이카운트는 ERP로, 온라인사업부 **가용 재고**(창고 `20002` 디아이 판매)를 들고 있다.
  실무 매뉴얼의 "재고1 → 출력물 → 재고현황 → 창고별재고현황 → 20002" 단계에 해당한다.

---

## 1. 무엇이 완료되었나

| 항목 | 상태 |
|---|---|
| 인증 연결 (Zone → 로그인 → SESSION_ID) | ✅ 완료·검증 |
| 재고현황 조회 API (창고별) | ✅ 완료·검증 (실서버 20002 창고 2,038건) |
| 정식(실서버) 인증키 발급 + IP 등록 | ✅ 완료 |
| 세션 캐싱 (호출한도 대응) | ✅ 구현 |

---

## 2. 이카운트 OpenAPI 인증 절차 (3단계)

```
1) Zone 조회   POST https://{sboapi|oapi}.ecount.com/OAPI/V2/Zone
               body: {"COM_CODE": "<회사코드>"}
               → 응답 Data.ZONE  (우리 회사 = "CC")

2) 로그인      POST https://{sboapi|oapi}{ZONE}.ecount.com/OAPI/V2/OAPILogin
               body: {"COM_CODE","USER_ID","API_CERT_KEY","LAN_TYPE":"ko-KR","ZONE"}
               → 응답 Data.Datas.SESSION_ID

3) 실제 API    POST https://{sboapi|oapi}{ZONE}.ecount.com/OAPI/V2/<경로>?SESSION_ID=<세션>
               body: API별 파라미터
```

- **도메인 구분:** `sboapi` = 테스트(샌드박스), `oapi` = 실서버(운영).
- **우리 회사 ZONE = `CC`** (Zone 조회로 자동 획득하므로 하드코딩 불필요).

---

## 3. 자격증명 (.env)

`.env` 파일 형식 (같은 폴더에 템플릿 제공):

```
ECOUNT_COM_CODE=661945          # 회사코드 (공통)
ECOUNT_USER_ID=JULIA            # 사용자 ID
ECOUNT_API_CERT_KEY=<인증키>     # ★ 별도 전달받아 입력 (보안상 이 문서엔 미기재)
ECOUNT_IS_TEST=false            # 실서버=false, 테스트=true
```

- **정식(실서버) 인증키:** 유효기간 2026-06-16 ~ 2027-06-15. 본사 담당자가 직접 전달.
- **테스트 인증키:** 유효 ~2026-06-30. 샌드박스(`sboapi`)용. 개발/디버깅 시 사용 가능하나
  **연속 재발급 시 예고 없이 차단**될 수 있음.
- 발급/관리: 이카운트 → 셀프서비스 → **API인증키발급** / **API인증현황** 화면.

---

## 4. ⚠️ 호출 한도 (Rate Limit) — 설계에 직접 영향

**실서버(oapi) 기준:**
| 동작 | 한도 |
|---|---|
| **로그인 + 전체 조회/현황** | **10분에 1회** |
| 단건 조회/현황 | 1초에 1회 |
| 저장(쓰기) | 10초에 1회 |

(테스트 서버는 모두 10초에 1회로 더 관대.)

→ **반드시 SESSION_ID를 캐싱**해 로그인을 아껴야 한다. 전체 재고는 한 번 받아
**스냅샷으로 저장 후 재사용**하는 구조로 가야 한다. 코드에 25분 세션 캐시가 구현돼 있다.
→ 실서버에서 로그인이 실패하면 **곧바로 재시도하지 말 것** (10분 한도 소진). 원인 고치고 대기.

---

## 5. IP 등록

- **실서버(oapi)는 이카운트 [IP등록]에 등록된 공인 IP에서만 접속 가능.**
- 본사 사무실 고정 IP: **`121.164.89.20`** (등록 완료).
- **같은 사무실(같은 인터넷 회선) PC는 공인 IP가 동일**하므로 추가 등록 불필요.
- 다른 사무실/외부망에서 쓰려면 그 PC의 공인 IP를 [IP등록]에 추가해야 한다.

---

## 6. 코드 구성 (같은 폴더)

| 파일 | 역할 |
|---|---|
| `ecount_config.py` | `.env`에서 ECOUNT_* 로드 (`load_ecount_config()` → `EcountConfig`) |
| `ecount_auth.py` | Zone 조회 · 로그인 · **SESSION_ID 캐싱** (`get_session()` 이 핵심 진입점) |
| `ecount_inventory.py` | 창고별 재고현황 조회 (`get_inventory_by_location()`, `extract_rows()`) |
| `test_ecount.py` | 연결 테스트 (Zone→로그인 단계별 통과 확인) |
| `query_ecount_inventory.py` | 재고 조회 실행 + 원본응답 `output/*.json` 저장 |
| `.ecount_session.json` | 세션 캐시 (자동 생성, 외부 공유 금지) |

- 핵심 진입점: `from ecount_auth import get_session` → `sess = get_session()` →
  `sess['base_url']`, `sess['session_id']` 로 임의의 OAPI 호출 조립.
- `get_session()` 은 `.env`의 `ECOUNT_IS_TEST` 가 바뀌면(테스트↔실서버) 캐시를 무시하고 새 로그인한다.

---

## 7. 재고현황 조회 API 상세

```
경로 : /OAPI/V2/InventoryBalance/GetListInventoryBalanceStatusByLocation
방식 : POST, URL 뒤에 ?SESSION_ID=<세션>
body : { "BASE_DATE": "YYYYMMDD", "WH_CD": "20002"(선택), "PROD_CD": "..."(선택) }
```

**응답 행(row) 필드 (검증된 실제 구조):**
| 필드 | 의미 | 예시 |
|---|---|---|
| `WH_CD` / `WH_DES` | 창고코드 / 창고명 | `20002` / `디아이 판매(온라인사업부)` |
| `PROD_CD` | 이카운트 품목코드 | `07872`, `0000000001718` |
| `PROD_DES` | 품목명 | `피그마 우산` |
| `PROD_SIZE_DES` | 규격명 | (품목명과 동일한 경우 많음) |
| `BAL_QTY` | 재고수량(문자열) | `"313.0000000000"` → **숫자 변환 필요** |

- 응답 래퍼: `{"Data": {...}, "Status": "200", "Error": ..., ...}`.
  실제 행 리스트 위치가 버전에 따라 다를 수 있어 `extract_rows()` 가 방어적으로 추출한다.
- **검증 결과:** 실서버에서 `--wh 20002` 로 **2,038건** 정상 수신.
- **참고:** 이카운트 테스트 샌드박스에도 실데이터 사본이 있어 테스트키로도 개발 가능
  (단 실시간 최신 보장은 아님).

---

## 8. 실행 방법

```bash
# (최초 1회) venv 생성 + 라이브러리 설치
python -m venv venv
venv\Scripts\python.exe -m pip install -r requirements.txt   # requests, python-dotenv

# 연결 테스트
venv\Scripts\python.exe test_ecount.py

# 재고 조회 (20002 창고)
venv\Scripts\python.exe query_ecount_inventory.py --wh 20002
```

- 비개발 담당자용으로 더블클릭 실행 파일(`1_최초설치.bat`, `2_연결테스트.bat`,
  `3_재고조회_20002.bat`)과 `설치안내.txt` 도 같은 폴더에 있다.
- 윈도우 콘솔 한글/이모지 깨짐 방지를 위해 스크립트가 stdout을 UTF-8로 강제한다.

---

## 9. 미해결 / 다음 단계

- [ ] **`PROD_CD`(이카운트 품목코드) ↔ 네이버 바코드(JAN) 매칭 규칙 확정.**
      PROD_CD가 13자리 JAN과 다른 형태(짧은 코드, 0패딩)가 섞여 있어 매핑 테이블/규칙 필요.
- [ ] **`-`(음수) 재고 이상치 자동 탐지** (실무 매뉴얼의 재고 점검 단계 자동화).
- [ ] **가용재고 계산** (이카운트 재고 vs 주문 대조) — 출고 시트 Sheet3 자동 채우기로 연결.
- [ ] 전체 재고 **스냅샷 캐시 저장** (10분 한도 대응).
- [ ] 필요 시 다른 이카운트 API(저장/쓰기 등)도 테스트키로 먼저 호출해 **검증 등록** 후 사용.

---

## 10. 핵심 주의사항 요약

```
✅ ZONE=CC, 회사코드 661945, 사용자 JULIA
✅ 실서버 로그인/전체조회 = 10분 1회 → 세션 캐싱 필수, 실패 시 재시도 자제
✅ 실서버는 등록 IP만 접속 (사무실 121.164.89.20, 같은 회선이면 OK)
✅ 인증키는 .env 에만 두고 문서/코드에 하드코딩 금지
✅ BAL_QTY 등 수량은 문자열 → 숫자 변환
✅ 테스트키 연속 재발급 시 차단 주의
```

*문서 끝.*
